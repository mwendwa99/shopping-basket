import React, { useState } from 'react';


const DeleteItem = ({ list, price, id }) => {



    return (
        <div>
            {/* <button type="button" onClick={() => handleRemove(thing.id)}>
            Remove
          </button> */}
        </div>
    )
}

export default DeleteItem
